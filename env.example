# ============================================================================
# Supabase Configuration (Production)
# ============================================================================
# Get these from: https://supabase.com/dashboard/project/_/settings/api
#
# 1. VITE_SUPABASE_URL: Your Supabase project URL
#    Format: https://xxxxxxxxxxxxx.supabase.co
#
# 2. VITE_SUPABASE_ANON_KEY: Your Supabase anon/public key
#    This is a long JWT token (starts with "eyJ...")
#    ⚠️ Use the anon/public key, NOT the service_role key
#
# 3. VITE_SUPABASE_STORAGE_BUCKET: Storage bucket for images
#    Default: wishlist-images (must be created in Supabase Storage)
#
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here
VITE_SUPABASE_STORAGE_BUCKET=wishlist-images

# ============================================================================
# Backend Mode
# ============================================================================
# Controls whether to use Supabase or mock backend (localStorage)
#
# - Set to 'true': Forces mock mode (no Supabase needed)
# - Set to 'false' or leave empty: Uses Supabase (requires credentials above)
#
# Mock mode is automatically enabled if Supabase credentials are missing.
# This is useful for:
# - Local development without Supabase
# - Testing and CI/CD pipelines
# - GitHub Pages deployment (static hosting)
#
VITE_USE_MOCK_BACKEND=false

# ============================================================================
# Base Path (for GitHub Pages deployment)
# ============================================================================
# Set the base URL path for your application
#
# - For root deployment (e.g., vercel.app): /
# - For GitHub Pages: /repository-name/
# - Example: /family-logistics-dashboard/
#
VITE_BASE_PATH=/

# ============================================================================
# In-app Issue Reporting (Server-side only)
# ============================================================================
# The app can create GitHub issues via a Supabase Edge Function.
# These variables MUST NOT be prefixed with VITE_ and MUST NOT be exposed
# to the frontend. Configure them as Supabase Function secrets:
#
#   supabase secrets set GITHUB_TOKEN=... GITHUB_OWNER=... GITHUB_REPO=...
#
# Required secrets:
# - GITHUB_TOKEN: GitHub token with permission to create issues in the repo
# - GITHUB_OWNER: Repository owner/org
# - GITHUB_REPO: Repository name
#
# GITHUB_TOKEN=
# GITHUB_OWNER=
# GITHUB_REPO=
