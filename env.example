# Supabase Configuration (optional - leave empty for mock mode)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_SUPABASE_STORAGE_BUCKET=wishlist-images

# Backend Mode
# Set to 'true' to force mock mode, or leave empty to auto-detect based on Supabase credentials
VITE_USE_MOCK_BACKEND=

# Base Path (for GitHub Pages deployment)
# Set to your repository name if deploying to GitHub Pages, e.g., /family-logistics-dashboard/
# Leave empty or set to / for root deployment
VITE_BASE_PATH=
